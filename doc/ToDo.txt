*Array ops
 matrix_times_matrix : DONE!

*MPI
 -synchronize ghosts

*interpolation, unless all nodes are the same

*refine and coarsen
 -interpolate data; from an to parent
  get_parent_data, get_children_data

*Legendre : DONE!

*DG

*Burgers



Plan1:
------
*make ghosts in g[6] of dat be the values obtained from the neighbor node.
 E.g. g[i][2] may have var i from neighbor node in -Y dir.
 g[i][2] then has e.g. n[0]=n[1]=4 (if nb has 4 points in X and Z dirs) and:
 n[2] = 1 if there is only one neighbor
 n[2] = 4 if there are 4 neighbors
*make 2d-array for u_other and use data in g to interpolate into u_other
*the node and each nb has to interpolate onto 16 points, load is balanced
*use u and u_other to set another 2d-array for flux
*use flux for time step
*this way each node only sends its own data on the surface, if e.g. 4 nb face
 one node (and each has n[0]=n[1]=4):
 node sends 4*16 points and 4 nb send 4*16 points
-> load is balanced, 8*16 points are sent

Plan0:
------
*make ghosts in g[6] of dat be the values interpolated by neighbor node.
 E.g. g[i][2] may have interpolated var i from neighbor node in -Y dir.
 g[i][2] then has e.g. n[0]=n[1]=4 (if nb has 4 points in X and Z dirs) and:
 n[2] = 1 if there is only one neighbor
 n[2] = 4 if there are 4 neighbors
*then u_other = g
*use u and u_other to set another 2d-array for flux
*use flux for time step
*this way each node only sends interpolated data, if e.g. 4 nb face
 one node (and each has n[0]=n[1]=4):
 node sends 4*16 points and 4 nb send 4*4 points
*node needs to interpolate values to 4*16 points
*each of the 4 nb has to interpolate only to 4 points (not balanced)
-> load is not balanced, node interpolats and sends more, 5*16 points are sent

=> Plan1 is more balanced, but Plan0 sends less


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
more:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Q:
How does wavelet AMR in DENDRO-GR work?
How does EinsteinToolkit do its great AMR?

*load balancing:
 -put equal numbers of nodes on each MPI process
 -if we add other stuff like neutrino transport, e.g. Blotzmann eqn we can
  1. add more entries to tNode regarding extra things
  2. add new struct for Boltzmann and distribute it

*OpenMP:
 -give many nodes to each MPI proc, use OpenMP loops over these loops
  on each proc

*change node list:
 -make also an array of nodes of myrank (to be looped oever with OpenMP for)
  create this array each time nodes are created or destroyed
  arrange all VarList loops such that the outermost loop is over this new array
 -save first entry of linked node list also in tNlist structure??? NO


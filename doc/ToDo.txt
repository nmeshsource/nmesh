2 good patch tests:
-------------------
1.:
*ini. mesh
*load balance
*again set up data like in SurfExchange_test
*output t=0
*exchange surfaces
*output t=1
*end

2.: (when iterpolation works)
*ini. mesh
*load balance
*set up some data profile
*output t=0
*remove some nodes
*load balance
*add some nodes
*load balance
*remove some nodes
*load balance
*output t=1
*end


Things that need to be done urgently:
=====================================
*Array ops
 matrix_times_matrix : DONE!

*MPI
 -synchronize ghosts : DONE!

*refine and coarsen
 -interpolate 3d data from and to parent
  get_parent_data   : DONE!
  get_children_data : DONE!
 -interpolate 3d data from and to higher level ancestor. We would need this
  if we add several levels with few points to catch a shock...

*Legendre : DONE!

*some output: DONE!

*test bfaces & fnb:  bfaces seem ok, fnb was missing some nb, but maybe ok now

*2d interpolation
 -interp from ghosts within patch     : DONE!
 -interp from ghosts of other patches : DONE!
  Q: Why is interp not correct for poly?
  A: We cannot have poly in lam,A,B on all patches because coords in diff
     patches are non-linear funcs of each other!
 -check if copy works across patch boundaries: seems ok

*evolve module in main: DONE!
-struct with slots for evo varlists & RHSs for several systems (Z4 + grhd)
-allow explicit time dependence in RHS
-keep open mind about node dependent dt

*decent 2d qnd 3d output: Bernd's XDMF: DONE!

*make some pars to decide what mesh we use: DONE!

*decide which pieces of the RK substep need to be limited!!!
 -in bamps it seems what I call w (and Bernd v) is limited after each
  substep BUT u itself is limited only at the very end!!!
 ==> for now proceed as in bamps as this looks very reasonable.

*make main/dg: DONE!
 -contains numerical fluxes (e.g. LLF and HLL from hrsc_numflx1d.c)
 -contains DG surface flux F = (f^* - f) * norm

*use main/dg with updated advection1: DONE!

*SSP RK3 from F. Hebert: DONE!

*extend evolve with limiters that transmit info in indicators: DONE!
 -check if MRS limiter works: seems ok.

*minmod and minmodB from K. Schaal: DONE!

*advection Eqn with DG: DONE!
 -check error: adv_conv_*/advection1_u_err_rms.t goes down with increasing n

*flux integral conservation
 -make integrals: DONE!
 -make 3d vol int.: DONE!

*add struct with nfaces: DONE!

*use MPI tags from surface.c also in indicators.c: DONE!

*implement dt calculation: DONE!

*hrefine_nodes_if_rflag needs to call create_children_no_nid_update in an
 order that is the SAME on all procs, otherwise the nfaces and fnb have
 differnt ordering and the nb index ni cannot be used as a MPI tag: DONE!

*checkpointing: DONE!
 -save entire mesh
  +save all patch info, so that we can call funcs to create them, with their
   bfaces and all
  +save all nodes on each level (can later go through them and create all
   nodes)
  +for leaf nodes save all enabled EvoVars, but not RK stage vars
 -call checkpoint_load after POST_PARAMETERS
 -call amr's setup_mesh diectly if we do not load checkpoint
 -make par checkpoint_save_pars. All pars in this list will be saved, all
  others come from parfile or earlier call to initialize_libraries
 -call checkpoint_save after ANALYZE
 -save checkpoint in a way that is independent from num. of MPI procs
 -unlike in BAM we do not need to make sure that each _init is called, e.g.
  calling all time bins up to at least PRE_INITIALDATA, or maybe doing one
  full evo step...
 -put checkpoint into dir checkpoint_new, then mv to checkpoint
 -save these files in checkpoint dir:
  patches.txt, nodes.txt, variables.bin, save_pars.txt

*implement:
 +TOV: DONE!
 +ADM: works for one TOV star
 +EOS: works

*TOVstar with refinement near surface:
 +invent criterion that detects interfaces between vacuum and matter regions
 +1st print ref-criterion in different places to see if it can find the
  star surface

*implement GRHD:
 -c2p: does something
 -positivity limiters
   find root for larger P
   make root finder with upper or lower limit
   make root finder with fd deriv
 -general limiter
 -check c2p with root finder for Wv as in arXiv:1306.4953

*add evolve_compute_change
 +set u_change = u before evo step: DONE!
 +set u_change = u - u_change after evo step: DONE!
 -should we divide u_change by dt??? ???

*check out:
 Anninos' work, e.g. COSMOSDG: https://iopscience.iop.org/article/10.3847/1538-4365/aa7ff5/pdf
 Dumbser

*implement/test limiters in 1 dir as in Hebert

*what is WENO3 in bamps?

*can we make a limiter by filtering?
 E.g. keep only 1, x,y,z, x^2,y^2,z^2,xy,xz,yz, x^3,y^3,z^3,x^2y,xy^2,xyz,...
 I.e. drop all that is higher than cubic

*advect square profile and check for flux integral conservation: seems ok

*check GRHD:
 -do tests in appendix of https://arxiv.org/pdf/1604.07999.pdf
 -TOV star in Cowling approx

*check if HLL flux is correct (compare with fv_HLL in
                               bamps' projects/grhdc/grhdc_FV.c)

*replace interpolation in surface communication by something like mortar meth
 -Bernd told me that I can read Kopriva's book or
  papers~\cite{Kopriva1996a,Kopriva2002a} about the mortar method
 -Insert a surface between adjacent leaf nodes with enough points so that
  transfer in at leat one direc. can be done without errors
 -there are 2 properties that one would like to have:
  global conservation & outflow condition
 -there are also works by Dumbser and Canuto about this

*check funs in refine.c
 +MPI part alone seems to work
 +threads alone seem to work if I use critical secs
 -test with MPI + threads, may need to init MPI differently...

*implement mesh refinement of troubled cells/nodes
 +go from n_old, n_new= 1 + n_old/2, until we reach n_new=1
  but only if newly created node is still troubled
 +check for troubled cells before each full evo step
  -can use indc & limiters to find troubled inside each MPI proc
  -then use nMPI_Ibcast to get troubled nodes to all ranks
  -refine each troubled node on all ranks, can we use omp tasks for this?

*read 1908.00655
 -info about gamma pars of GHG
 -add similar neutrino treatment to nmesh

*Burgers

*DG with 2nd oder wave eqn

*decide what we use to compute surface fluxes sqrt(det2gamma) or g^ii

*check if set_ajsurf_forall_vars should be called in get_all_surfaces
 or if we should rather call set_all_myln_ajsurf in an OpenMP loop
 afterwards

*DG with 3-metric
 -I think node_normal_at_ijk then is just n_i. Need metric to get n^i...
 -Is this needed? Can we redefine \sqrt{\gamma} u -> u ?

*DG and metric identities
 -do we need a non-diagonal mass matrix?
   In Teukolsky there is no big diff.,
   but in Bugner's thesis mass cons improves... ???
 -Liwei told me that a diagonal mass matrix is stupid according to...

*in main/amr/connect.c we set and read ->nc_lock. Do we need a omp flush
 before every read and a omp flush after every write, even though we are
 in omp_set_lock region???


*IDEA about ghost zones for finite differences
 -make main/amr/ghostupdate.c analog to main/amr/surface.c
 -in main/amr/ghostupdate.c we make analogs to:
    set_all_myln_mysurf(mesh);
    request_all_myln_surfaces_exchange(mesh);
    get_all_myln_surfaces(mesh);
 -in evolve and amr/load we then call funtions that either call
  set_all_myln_mysurf,request_all_myln_surfaces_exchange,get_all_myln_surfaces
  from main/amr/surface.c or their analogs from main/amr/ghostupdate.c
 -main/amr/ghostupdate.c should first exchange data in the X-dir, then the
  Y-dir, and finally in the Z-dir. In this way we can communicate data from
  next-next nearest neighbors via the arrays in tSurface.


Questions about bamps:
======================
*Wave Eqn:
  -boundary
  -can it work with LLF flux
*MPI
  -what is better MPI send/rcev or one-sided comm.?
    Bernd: there are papers that criticize one-sided comm.
           on many platforms MPI implementations for one-sided do the same
           as two-sided comm. behind the scenes
*OpenMP:
  -how to improve scaling? how to reduce thread creation overhead?
*How to benchmark MPI or OpenMP?
*GRHD:
  +Vivek can do single TOV star: DG only (no fin.vol.) one box, 8 or more
   DG elements, 4 or 5 points per element
  -What limiters do you use?
    Entropy based limiter (needs extra EoM).
    This is a flux limiter: use LLF if entropy is high, otherwise HLL
    *can you use LLF everywhere?
  -How do you do C2P?
    standard C2P
  -Do you use art. atmosphere?
    Yes, similar as BAM, but T_{\mu\nu}=0 in cons. eqns sources terms when
    rho is at atm level, cons vars are computed from prims at atm level
  -What outer BCs?
  -What internode BCs? Is LLF good enough?
    not clear
*GR
  -What systems are working?
  -Is filtering needed?
    Bernd: yes even for wave eqn if several boxes
           there is a standard exponential filter that pretty much only
           filters the highest coeff
*AMR:
  -What refinement criterions do you use?
  -What load balancing scheme?
    each node gets a weight (depending on e.g. how many grid point it has)
    then all nodes are distributed such that all proc have same
    load (calculated using the weights)
  -How to do non-uniform time steps?
*IO
  -binary XMF seems slow
    they have option to have binary data in one file, or one file/node
    Sara said we need the right order of spatial and temporal in xmf file
  -VisIt has problems showing mesh when refinement/derefinement happens
    they use paraview


Things that should be done:
===========================
*does hrefine_mesh_to_level need a fornodelist(mesh->lns, el) loop or can we
 use formylnodes(mesh):
 -The way hrefine_mesh_to_level is curently written, it needs fornodelist!
  Reason: hrefine_nodes_if_rflag needs to be called on each MPI job, but
  ref=0 on one MPI job is possible if it has no nodes to be refined.
 -But it is possible to use formylnodes together with hrefine_nodes_if_rflag!
 -Probably hrefine_pat would already work as is with formylnodes

*consider moving some stuff from tNode into tDat, e.g.:
 int rflag; nMPI_Comm comm;

*consider removing entries from tNode, e.g.: leaf, np ???

*consider adding a "deflated" flag to tNode. If yes set then all node data
 needs to be gotten from MPI proc with datrank. A deflated node would not
 have any node info set, and need less memory.
 BUT for this to work we probably need to replace things like
  struct tNODE *parent;
 in tNode by
  char *parentname;
 I.e. use full names instead of pointers. Is this worth it???

*fix n0=n1=n2=1 case where set_ajsurf_forall_vars seems to interpolate
 -seems to interpolate only if the 2 nodes are in diff. patches
  which is rare, and thus we maybe should not worry about this...

*investigate: MPI_Test and MPI_Iprobe as alt. for MPI_Wait (as in Athena++)

*I think get_all_surfaces takes very long on koko with 8^6 node GRHD:
  -maybe use MPI_Test and MPI_Iprobe to avoid MPI_Wait (as in Athena++)
  -we could work on set_ajsurf_forall_vars for which ever requests are
   done instead of waiting for all of them in order...
 +do same for corresponding indc exchanges...

*in main/amr/storage.c we often set mesh->lns = first_nodelist(...
 -BUT in many cases we know that mesh->lns is unchanged, or even know
  how it chanched! : DONE in most often used parts.

*node->fnb is an array, now there is also a nodelist in node->nfaces[f]
 -should we get rid of node->fnb???

*we have timer.c to see what needs to be optimized
 -make timer_get s.t. "name" gets appended by threadID: DONE in some way.
  -but should we use gettid? or some openMP call with #ifdef around it???

*compare with: http://www.cfca.nao.ac.jp/files/um2014_tomida.pdf

*investigate: tasks within each node using OpenMP:
              https://openmp.org/wp-content/uploads/sc13.tasking.ruud.pdf

*investigate: Adams-Bashforth methods. Then we need e.g. 3 timelevels.
              Spectre uses this so local timesteps are easier.

*investigate: Spectre uses Hermite WENO (HWENO)

*investigate new c2p: https://arxiv.org/abs/2005.01821

*update set_all_myln_mysurf and request_all_myln_surfaces_exchange
 so that we also have min/max or averages in extra space of mysurf
 and nbsurf arrays <-- will NOT work!!! Because in a RK substep we need
 to limit w = u_p + r dt and not r or u_p.

*add to tNode:  int nchildren; // number of children this node has, usually 8
  -update of storage.c should be easy
  -update of connect.c will be harder because it assumes nb in all 3 direc.

*checkpoint_load_nodes and checkpoint_load_Vars do serial reads, one
 MPI proc after the other. Maybe we want parallel MPI_File_read_all .


Cleanup:
========
-remove ldescendants_along_face
-remove/fix/replace: mark_points_in_nb_f
-remove some comment and if(0) blocks
-fix warning. The line
  if(fatal) errorexits("Could not find parameter \"%s\"", name);
 in findparameterindex causes an incorrect warning with -O3:
 warning: ‘__s’ may be used uninitialized in this function [-Wmaybe-uninitialized]
 parameters.c: note: ‘__s’ was declared here
  int iterate_parameters"

Optimizations:
==============
*mm_array2 is 4 times slower than mm_array1 and mm_array1 is 4 times slower
 than mm_array2
 -would this help?: #define Ind_n0n1(i,j,k,n) ( (i) + (n0)*( (j) + (n1)*(k) ) )
 -try stack arrays: 10% speed gain on tyrfing
 -try changing i,j,k loop orders: no speed gain on tyrfing
*MeshVarInd is slow
 -change Mathematica to declare var indices ouside all loops: DONE!
 -use klib's khash
*move init_all_myln_myindc_for_vl and its free out of evostep: DONE!

-----------------------------------------------------------------

Parallelization:
================
1. use MPI for communication between nodes

2. use OpenMP within each node
*use "omp parallel for" for some loops, mainly in formylnodes
*use "omp task" as in wolfGIT/c/tasktest_omp2.c inside loops like fornodelist
 -most interpolations are local and can be indpendent tasks
 -calls to coordinates_init_node can be always made omp tasks
 -setting ajsurf could be a task or be inside an omp loop
 -is omp loops don't scale in formylnodes, we can try one task per node...



-----------------------------------------------------------------

MPI parallelization plans

Plan1:
------
*make ghosts in s[6] of dat be the values obtained from the neighbor node.
 E.g. s[2][i] may have var i from neighbor node in -Y dir.
 s[2][i] then has e.g. n[0]=n[2]=4 (if nb has 4 points in X and Z dirs) and:
*make 2d-array for u_other and use data in s to interpolate into u_other
*the node and each nb has to interpolate onto 16 points, load is balanced
*use u and u_other to set another 2d-array for flux
*use flux for time step
*this way each node only sends its own data on the surface, if e.g. 4 nb face
 one node (and each has n[0]=n[1]=4):
 node sends 4*16 points and 4 nb send 4*16 points
-> load is balanced, 8*16 points are sent

Plan0:
------
*make ghosts in s[6] of dat be the values interpolated by neighbor node.
 E.g. s[2][i] may have INTERPOLATED var i from neighbor node in -Y dir.
 s[2][i] then has e.g. n[0]=n[2]=4 (if nb has 4 points in X and Z dirs) and:
*then u_other = s
*use u and u_other to set another 2d-array for flux
*use flux for time step
*this way each node only sends interpolated data, if e.g. 4 nb face
 one node (and each has n[0]=n[2]=4):
 node sends 4*16 points and 4 nb send 4*4 points
*node needs to interpolate values to 4*16 points
*each of the 4 nb has to interpolate only to 4 points (not balanced)
-> load is not balanced, node interpolates and sends more, 5*16 points are sent

=> Plan1 is more balanced, but Plan0 sends less

=> in nmesh I went for Plan1!!!

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
more:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Q:
How does wavelet AMR in DENDRO-GR work?
How does EinsteinToolkit do its great AMR?

*load balancing:
 -put equal numbers of nodes on each MPI process
 -if we add other stuff like neutrino transport, e.g. Blotzmann eqn we can
  1. add more entries to tNode regarding extra things
  2. add new struct for Boltzmann and distribute it

*If we ever regret using MPI, as opposed to e.g. Chram++
 +consider: AMPI - Adaptive Message Passing Interface

*OpenMP:
 -give many nodes to each MPI proc, use OpenMP loops over these loops
  on each proc
 -use OpenMP in formylnodes

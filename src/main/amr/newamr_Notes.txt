General:
========
*maybe typdef only tElm and then
#define tNode tElm


MPI exchange:
=============
| rank0         | rank1         | rank2         |
|elmA  ...  elmB|elmA  ...  elmB|elmA  ...  elmB|

Make an info exchange function (elm_exchange_between_nbranks):
 -exchange first and last elm between rank_i and rank_i+1,
  and also some aggregate numbers such as:
  +number of elms on each rank
  +total speed and nodeload sums as in load_set_nodeload_array for each rank
 -save this info for rank on left and right in tMesh.

*This way we have ref flag of next rank and know if sibling on other side
 exists and e.g. needs to be de-refined.


About load bal:
===============
*make load bal func for elms first, because this is needed for
 remove_elms_if_rflag, where we need all 8 on same rank to de-refine.
*maybe keep all 8 siblings together on 1 MPI rank, then any AMR criterion
 can check locally (without MPI communication) if de-refinement (8sib->1parent)
 should happen.
 -but not, if one of the siblings gets refined further
 -how do we store this info about other siblings???
  +could store a flag sib_ref that says if sibling is refined???
  -sounds complicated, maybe don't keep them together after all???
  +or have flag keep_together=1 on each sibling that gets zero'd if
   one is later refined? But how does this flag get 1 again?
=> maybe don't try to keep them together:
    Rather make AMR-func that checks all 8 and sets ref flag to de-ref.
    Then a loadbal func can gather all 8, if all 8 agree that they
    should be de-refined.
    *check how destroy_nodes_no_nid_update currently works,
     do it compare ref flags on all 8???


About refinement:
=================
*we should be able to do local refines now,
 just nb lists and nid's need to be updated afterwards

*make new refinement funcs:
 e.g. make new:
   hp_refine_nodes_if_rflag -> hp_refine_elms_if_rflag
   remove_nodes_if_rflag    -> remove_elms_if_rflag??? <-not done yet
 the 2 old ones also needed:
   create_children_no_nid_update
   destroy_nodes_no_nid_update
 but the new versions operate more rank local, and may not them!

*de-refine is trickier. We  can remove all 8 siblings only if none are
 refined themsleves AND if ALL are flagged for de-ref.
 +check among all locally available siblings and set their ref flags to zero
  if any one sibling has ref=0.
 +then call elm_exchange_between_nbranks from above, and again zero ref
  flags if siblings on nb rank have zero flag, or are refined themselves...

BUGS:
=====
*In the master branch (at e4a071f6a) there is a bug in the bfaces.
 It can be triggered by changing src/main/amr/mesh.c . This change is
 in the branch master_bug (at 1c705303). We can get it with:
   git checkout master_bug


General:
========
*maybe typdef only tElm and then
#define tNode tElm


MPI exchange:
=============
| rank0         | rank1         | rank2         |
|elmA  ...  elmB|elmA  ...  elmB|elmA  ...  elmB|

Make an info exchange function (elm_exchange_between_nbranks):
 -exchange first and last elm between rank_i and rank_i+1,
  and also some aggregate numbers such as:
  +number of elms on each rank
  +total speed and nodeload sums as in load_set_nodeload_array for each rank
 -save this info for rank on left and right in tMesh.

*This way we have ref flag of next rank and know if sibling on other side
 exists and e.g. needs to be de-refined.


About load bal:
===============
*make load bal func for elms first, because this is needed for
 remove_elms_if_rflag, where we need all 8 on same rank to de-refine.
*maybe keep all 8 siblings together on 1 MPI rank, then any AMR criterion
 can check locally (without MPI communication) if de-refinement (8sib->1parent)
 should happen.
 -but not, if one of the siblings gets refined further
 -how do we store this info about other siblings???
  +could store a flag sib_ref that says if sibling is refined???
  -sounds complicated, maybe don't keep them together after all???
  +or have flag keep_together=1 on each sibling that gets zero'd if
   one is later refined? But how does this flag get 1 again?
=> maybe don't try to keep them together:
    Rather make AMR-func that checks all 8 and sets ref flag to de-ref.
    Then a loadbal func can gather all 8, if all 8 agree that they
    should be de-refined.
    *check how destroy_nodes_no_nid_update currently works,
     do it compare ref flags on all 8???


About refinement:
=================
*we should be able to do local refines now,
 just nb lists and nid's need to be updated afterwards

*make new refinement funcs:
 e.g. make new:
   hp_refine_nodes_if_rflag -> hp_refine_elms_if_rflag
   remove_nodes_if_rflag    -> remove_elms_if_rflag??? <-not done yet
 the 2 old ones also needed:
   create_children_no_nid_update
   destroy_nodes_no_nid_update
 but the new versions operate more rank local, and may not need them!

*de-refine is trickier. We  can remove all 8 siblings only if none are
 refined themsleves AND if ALL are flagged for de-ref.
 +check among all locally available siblings and set their ref flags to zero
  if any one sibling has ref=0.
 +then call elm_exchange_between_nbranks from above, and again zero ref
  flags if siblings on nb rank have zero flag, or are refined themselves...
 +OR: just set elm et contrib to myT to 0 if(elm_get_ijk(elm)!=0)
  But NOTE: in load_balance_elms there is this if:
            "if(et <= loadTmin) et = loadTmin;" which could screw this up!!!



More about load bal:
====================
on one rank r we have:
T_r = time to do something (e.g. a RK step) on all nodes on this rank
T_r = \sum_elm t_elm
v_r = speed of rank
ops_r = v_r * T_r = # of operations actually done on this rank

total ops:
ops = \sum_r ops_r

distribution weights:
w_r = v_r / (\sum_ri v_ri)

*let's give each rank this many ops
ops_bal_r = w_r * ops
*on rank: loop over elms until  ops = v_r \sum_elm t_elm  reaches ops_bal_r

*from ops_r get:
 ops_tot_r = \sum_{ri=0}^r ops_ri
 we build this by sending ops_tot_r from r to r+1


About nid:
==========
*use maybe: unsigned long nid; //not size_t because we don't have MPI_SIZE_T
*then also:
 #define  NID_INVALID  (ULONG_MAX)
 #define  NID_REMOVED  (ULONG_MAX-1)
 (these 2 are -1 and -2 when converted to long)
*also add oid to save old value of nid (e.g. after refine)
*make nids again, in a way that just numbers all elms over all ranks
 consecutively
*then nids do not change because of load bal
*also form the nid alone we can tell the rank, if we store the 1st nid of
 every rank in the tMesh on each rank;

About refine:
=============
*if we refine certain nids get removed
*nids only need to be updated after we refine
*to refine we remove the parent from mesh->myelm_head and set the
 corresponding pointer in mest->myelm to NULL

About fnb info and such:
========================
*instead of node->nfaces introduce fnbid[6], make this a regular var:
  AddAuxVar("amr_elm_fnbid", "@", "vars that contain arrays for *fnbid[6]");
  MeshVarSetType(mesh, Ind("amr_elm_fnbid0"), EXCHVAR); //to send via MPI
  //change refine-interpolation such that EXCHVARs get skipped like AUXVARs
 fnbid[f] contains unsigned long // e.g. fnbid[f][3] = nid of nb #3 on face f
*fnbid needs to be set using the nb elm finding funcs I made
*from fnbid we can then set the elm->fnb pointers
*we also copy all elms from other ranks that are nb of one of our elms
 we put them into either: a linked elm list: struct list_head nbelm_head;
                          or an array-list:  tElm **nbelm;
*when we refine an elm:
 -signal all nb elms that the elm is gone now by setting the
  corresponding nb->fnb pointer to NULL.
 -all rank local elms that have fnbid[f] but with a NULL nb->fnb[f] need
  to update their entire fnbid[f] and nb->fnb[f]
 -Since there need to be all kinds of changes to the nbelm list anyway,
  we just erase it.
  But before we do so we write a NULL into corresponding elm->fnb pointers
  of all my elms, to signal that their fnbid[f] & fnb[f] need to be updated.
 -now we have only our own elms, but lots of them have missing nb info
 -next we update all elm->nid everywhere (may save the old nids in elm->oid)
 -update all fnbid[f] where fnb[f] is not NULL by reading new nid via fnb[f]
  and invalidate all fnbid[f] where fnb[f] is NULL
 -Then update fnbid[f] for all faces of my elms that have an invalid fnbid[f].
  This will need MPI to find nb elms on other ranks!
 -PROBLEM: afterward we want to do load bal, that will probably erase nbelm
           as above. It will also invalidate a lot of elm->fnb.
           On the other hand fnbid[f] remains valid, so it should be
           sent via MPI (like regular vars are!)
 -the load bal should update all missing elm->fnb from fnbid

Load bal and fnb info:
======================
*the elms from other ranks in nbelm need to be erased, we should NULL all
 elm->fnb that point to them
*how do we get the fnbid[f] from other ranks???:
 make vars that contain the 6 vars fnbid[0-5]:
  AddAuxVar("amr_elm_fnbid", "@", "vars that contain arrays for *fnbid[6]");
  MeshVarSetType(mesh, Ind("amr_elm_fnbid0"), EXCHVAR); //to send via MPI
  //change refine-interpolation such that EXCHVARs get skipped like AUXVARs
 -it should be safe to redim the arrays in amr_elm_fnbid0-5
*since we still have complete fnbid[f] after balancing, recreate all missing
 elm->fnb from them
*also get copies of elms from other ranks, that are nb of any of my elms
*then update all nb info (in the elms copied from other ranks we may not
 need fnbid)

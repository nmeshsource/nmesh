General:
========
*maybe typdef only tElm and then
#define tNode tElm


About refinement:
=================
*we should be able to do local refines now,
 just nb lists and nid's need to be updated afterwards

*in create_children_no_nid_update of main/amr/refine.c :
 remove diff with master ( git diff master main/amr/refine.c ), i.e this:
+  /* replace all parent elms we have on this proc with their children */
+  NODELEVEL_Pragma(omp parallel)
+  {
+    ...
+  }

*make new refinement funcs:
 e.g. make new:
   hp_refine_nodes_if_rflag -> hp_refine_elms_if_rflag
   remove_nodes_if_rflag    -> remove_elms_if_rflag???
 the 2 old oned also needed:
   create_children_no_nid_update
   destroy_nodes_no_nid_update
 but the new versions operate more rank local, and may not them!
